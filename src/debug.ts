import { getKeyAndTopicsFromText } from './utils';

// console.log(createDatasetFromMessages(['hello', 'world']));

console.log(
  getKeyAndTopicsFromText('x: Persistent cough, Shortness of breath, Fever'),
);
